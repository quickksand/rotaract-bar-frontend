/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import {HttpClient, HttpContext, HttpResponse} from '@angular/common/http';
import {Observable} from 'rxjs';
import {filter, map} from 'rxjs/operators';
import {StrictHttpResponse} from '../../strict-http-response';
import {RequestBuilder} from '../../request-builder';

import {EvaluationSummary} from '../../models/evaluation-summary';

export interface GetEvaluationSummary$Params {

/**
 * Filter by year (e.g. 2025)
 */
  year?: number;

/**
 * Filter from this timestamp (inclusive)
 */
  from?: string;

/**
 * Filter until this timestamp (inclusive)
 */
  to?: string;

/**
 * Number of cups available at the start of the event
 */
  startCups?: number;

/**
 * Deposit amount per cup in EUR
 */
  depositAmountPerCup?: number;
}

export function getEvaluationSummary(http: HttpClient, rootUrl: string, params?: GetEvaluationSummary$Params, context?: HttpContext): Observable<StrictHttpResponse<EvaluationSummary>> {
  const rb = new RequestBuilder(rootUrl, getEvaluationSummary.PATH, 'get');
  if (params) {
    rb.query('year', params.year, {});
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
    rb.query('startCups', params.startCups, {});
    rb.query('depositAmountPerCup', params.depositAmountPerCup, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EvaluationSummary>;
    })
  );
}

getEvaluationSummary.PATH = '/api/evaluation/summary';
