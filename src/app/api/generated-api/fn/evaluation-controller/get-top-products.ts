/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import {HttpClient, HttpContext, HttpResponse} from '@angular/common/http';
import {Observable} from 'rxjs';
import {filter, map} from 'rxjs/operators';
import {StrictHttpResponse} from '../../strict-http-response';
import {RequestBuilder} from '../../request-builder';

import {ProductSalesEntry} from '../../models/product-sales-entry';

export interface GetTopProducts$Params {

/**
 * Filter by year (e.g. 2025)
 */
  year?: number;

/**
 * Filter from this timestamp (inclusive)
 */
  from?: string;

/**
 * Filter until this timestamp (inclusive)
 */
  to?: string;

/**
 * Maximum number of results to return
 */
  limit?: number;

/**
 * Sort order: desc = best sellers first, asc = worst sellers first
 */
  sortOrder?: 'asc' | 'desc';
}

export function getTopProducts(http: HttpClient, rootUrl: string, params?: GetTopProducts$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ProductSalesEntry>>> {
  const rb = new RequestBuilder(rootUrl, getTopProducts.PATH, 'get');
  if (params) {
    rb.query('year', params.year, {});
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
    rb.query('limit', params.limit, {});
    rb.query('sortOrder', params.sortOrder, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ProductSalesEntry>>;
    })
  );
}

getTopProducts.PATH = '/api/evaluation/top-products';
